<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Reclamezz" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="titre" >
        <!-- Customize your theme here. -->
        <item name="android:textColor">#14A2E4</item>
        <item name="android:textSize">40dp</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:layout_marginBottom">20dp</item>
    </style>
    <style name="btnStyle">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/bleu</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">17dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginLeft">3dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_marginRight">3dp</item>
        <item name="android:layout_marginTop">20dp</item>
    </style>
    <style name="Theme.Reclamezz" parent="Base.Theme.Reclamezz" />
</resources>